# 国际化实现规范

## 1. 概述

本项目使用 **next-intl** v3.0.0 实现国际化，支持中文（默认）和英文两种语言。

### 核心特点
- **路由策略**: 中文无前缀，英文使用 `/en` 前缀 (`localePrefix: 'as-needed'`)
- **混合风格策略**: UI 简洁，内容诗意
- **架构**: Next.js App Router + [locale] 动态路由

---

## 2. 文件结构

```
my-app/
├── messages/
│   ├── zh.json          # 中文翻译
│   └── en.json          # 英文翻译
├── src/
│   ├── i18n.ts          # i18n 配置
│   ├── middleware.ts    # 路由中间件
│   ├── navigation.ts    # 导航工具
│   └── app/
│       ├── [locale]/    # 国际化路由
│       │   ├── page.tsx
│       │   ├── layout.tsx
│       │   └── ...
│       └── layout.tsx   # 根布局
└── next.config.js       # Next.js 配置
```

---

## 3. 配置文件

### 3.1 i18n.ts
```typescript
import { getRequestConfig } from 'next-intl/server';
import { routing } from './navigation';

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;
  
  if (!locale || !routing.locales.includes(locale as typeof routing.locales[number])) {
    locale = routing.defaultLocale;
  }

  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default
  };
});
```

### 3.2 middleware.ts
```typescript
import createMiddleware from 'next-intl/middleware';
import { routing } from './navigation';

export default createMiddleware(routing);

export const config = {
  matcher: ['/((?!api|_next|.*\\..*).*)']
};
```

### 3.3 navigation.ts
```typescript
import { createNavigation } from 'next-intl/navigation';

export const locales = ['zh', 'en'] as const;
export const defaultLocale = 'zh';

export const routing = {
  locales,
  defaultLocale,
  localePrefix: 'as-needed'  // 中文无前缀，英文有 /en 前缀
};

export const { Link, redirect, usePathname, useRouter } = createNavigation(routing);
```

### 3.4 next.config.js
```javascript
const createNextIntlPlugin = require('next-intl/plugin');

const withNextIntl = createNextIntlPlugin('./src/i18n.ts');

/** @type {import('next').NextConfig} */
const nextConfig = {
  // 其他配置...
};

module.exports = withNextIntl(nextConfig);
```

---

## 4. 翻译文件规范

### 4.1 文件结构

```json
{
  "metadata": {
    "title": "页面标题",
    "description": "页面描述"
  },
  "brand": {
    "name": "品牌名",
    "tagline": "标语"
  },
  "nav": {
    "explore": "探索",
    "sanctuary": "庇护所",
    "timemachine": "时光机",
    "home": "首页",
    "back": "返回",
    "backToHome": "返回首页"
  },
  "home": {
    "subtitle": "副标题",
    "features": {
      "privacy": {
        "title": "标题",
        "desc": "描述"
      }
    }
  },
  "errors": {
    "spreadNotFound": "错误信息"
  }
}
```

### 4.2 命名规范

| 类型 | 示例 | 说明 |
|------|------|------|
| 页面级 | `home.title`, `spreads.subtitle` | 页面名称 + 键名 |
| 组件级 | `nav.back`, `wallet.connect` | 组件名 + 动作 |
| 动态内容 | `select.positionActive` | 使用参数插值 |
| 数组 | `journal.wordCards` | 使用 `t.raw()` 读取 |
| 枚举 | `spreads.types.single.name` | 嵌套结构 |

### 4.3 参数插值

```json
{
  "greeting": "你好，{name}！",
  "items": "你有 {count} 条消息"
}
```

使用：
```typescript
t('greeting', { name: 'Alice' })
t('items', { count: 5 })
```

---

## 5. 代码使用规范

### 5.1 基础用法

```typescript
import { useTranslations } from 'next-intl';

export default function Component() {
  const t = useTranslations();
  
  return <h1>{t('home.title')}</h1>;
}
```

### 5.2 动态键名

```typescript
const spreadType = 'single';
t(`spreads.types.${spreadType}.name`)
```

### 5.3 数组读取

```typescript
const words = t.raw('journal.wordCards');
words.map((word: string) => <span>{word}</span>)
```

### 5.4 带参数

```typescript
t('select.instruction', { count: spread.cardCount })
```

### 5.5 Server Component 中的使用

```typescript
import { getTranslations } from 'next-intl/server';

export default async function Page({ params }: { params: { locale: string } }) {
  const t = await getTranslations({ locale: params.locale, namespace: 'home' });
  
  return <h1>{t('title')}</h1>;
}
```

### 5.6 Metadata

```typescript
import { getMessages } from 'next-intl/server';
import type { Metadata } from 'next';

export async function generateMetadata({ 
  params 
}: { 
  params: { locale: string } 
}): Promise<Metadata> {
  const messages = await getMessages({ locale: params.locale });
  
  return {
    title: messages.metadata.title,
    description: messages.metadata.description
  };
}
```

---

## 6. 语言切换器实现

### 6.1 组件代码

```typescript
'use client';

import { usePathname } from 'next/navigation';
import { useLocale } from 'next-intl';
import { locales, type Locale } from '@/navigation';

export default function LocaleSwitcher() {
  const locale = useLocale();
  const pathname = usePathname();

  const switchLocale = (newLocale: Locale) => {
    if (newLocale === locale) return;

    const segments = pathname.split('/');
    
    if (segments[1] === locale) {
      segments[1] = newLocale;
    } else {
      segments.splice(1, 0, newLocale);
    }
    
    const newPath = segments.join('/') || `/${newLocale}`;
    window.location.href = newPath;  // 使用 window.location.href 确保页面刷新
  };

  return (
    <div className="flex items-center gap-1 text-xs">
      {locales.map((lang, index) => (
        <span key={lang} className="flex items-center">
          <button
            onClick={() => switchLocale(lang)}
            className={`
              px-2 py-1 transition-colors
              ${locale === lang 
                ? 'text-accent font-medium' 
                : 'text-muted hover:text-text'
              }
            `}
          >
            {lang === 'zh' ? '中' : 'EN'}
          </button>
          {index < locales.length - 1 && (
            <span className="text-muted">|</span>
          )}
        </span>
      ))}
    </div>
  );
}
```

### 6.2 重要说明

- 必须使用 `window.location.href` 而非 `router.push`
- 原因：语言切换需要重新获取服务器端数据，确保翻译及时更新

---

## 7. 路由和链接

### 7.1 使用 next-intl 的 Link

```typescript
import { Link } from '@/navigation';

<Link href="/spreads">{t('nav.explore')}</Link>
// 自动处理 locale 前缀
```

### 7.2 编程式导航

```typescript
import { useRouter } from '@/navigation';

const router = useRouter();
router.push('/spreads');
// 自动处理 locale 前缀
```

---

## 8. 翻译风格指南

### 8.1 混合策略

| 场景 | 中文风格 | 英文风格 |
|------|----------|----------|
| UI 元素 | 简洁 | 简洁 |
| 品牌标语 | 诗意 | 诗意 |
| 功能描述 | 清晰 | 清晰 |
| 错误提示 | 友好 | 友好 |

### 8.2 示例

```json
// zh.json
{
  "brand": {
    "name": "植物系疗愈 OH 卡",
    "tagline": "在 Web3 的世界里，种下属于你的疗愈之树"
  }
}

// en.json
{
  "brand": {
    "name": "Flora Healing OH Cards",
    "tagline": "Plant your healing tree in the Web3 world"
  }
}
```

---

## 9. 最佳实践

### 9.1 DO
- ✅ 使用嵌套结构组织翻译键
- ✅ 对重复文本使用参数插值
- ✅ 在 Server Components 中使用 `getTranslations`
- ✅ 在 Client Components 中使用 `useTranslations`
- ✅ 使用 `t.raw()` 读取数组
- ✅ 保持翻译文件结构一致

### 9.2 DON'T
- ❌ 在翻译键中使用动态值
- ❌ 混合使用不同风格的翻译
- ❌ 忘记处理 locale 前缀
- ❌ 使用 `router.push` 进行语言切换
- ❌ 在 JSON 中重复定义相同的键

---

## 10. 添加新语言

1. 创建 `messages/{locale}.json`
2. 复制 `messages/zh.json` 结构
3. 翻译所有值
4. 在 `navigation.ts` 中添加新 locale
5. 更新 `LocaleSwitcher` 组件

---

## 11. 常见问题

### Q: 翻译没有更新？
A: 语言切换使用 `window.location.href` 确保页面完全刷新。

### Q: 动态路由参数如何翻译？
A: 使用 `t()` 的参数插值功能，如 `t('welcome', { name: params.name })`。

### Q: 如何处理富文本？
A: 使用 `t.rich()` 方法，支持 HTML 标签。

---

## 12. 更新记录

### 2025-03-23
- ✅ 新增 `modal.closeHint` 翻译键（大图弹窗关闭提示）
- ✅ 修复 LocaleSwitcher 使用 `window.location.href` 而非 `router.push`
- ✅ 合并 messages 中重复的 nav 定义
- ✅ 新增所有牌阵位置名称翻译（select.position.*）
- ✅ 新增 `select.doubleClickToView` 翻译键

---

## 13. 参考

- [next-intl 文档](https://next-intl.dev/docs/getting-started/app-router)
- [Next.js App Router](https://nextjs.org/docs/app)
